import labelForm from "./src/components/InputForm.vue";
const addNew = () => {
  // 获取用户输入的数据，假设你有相应的输入框的值
  const userId = labelForm.userId;
  const userName = labelForm.userName;
  const userPhone = labelForm.userPhone;
  const userAddress = labelForm.userAddress;
  const userText = labelForm.userText;
};
function addNew() {}

export { addNew };
